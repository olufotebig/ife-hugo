{
  "name": "ife-hugo",
  "version": "0.0.1",
  "description": "Ife Hugo is a Hugo boilerplate for creating websites!",
  "main": "index.js",
  "scripts": {
    "build":
      "PUBLIC=public brunch build && hugo --config site/config.toml -s site/",
    "dev":
      "PUBLIC=dev brunch watch & hugo --config site/config.toml -s site/ -d ../dev/ -w & browser-sync start --server 'dev' --files 'dev'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-brunch": "^6.1.1",
    "babel-preset-env": "^1.6.0",
    "brunch": "^2.10.10",
    "hugo-bin": "^0.14.0",
    "sass-brunch": "^2.10.4"
  }
}
